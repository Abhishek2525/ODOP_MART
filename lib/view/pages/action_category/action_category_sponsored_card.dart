import 'package:flutter/material.dart';

import '../dashboard/cards/sponsored_movie_card.dart';

class ActionCategorySponsoredCard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    if (true) {
      return SponsoredMovieCard();
    }
  }
}
